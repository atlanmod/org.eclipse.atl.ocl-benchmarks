-- @atlcompiler emftvm
query TestSymmetricDif = thisModule.take(thisModule.numTakeIn, thisModule.squares(700)->select(e | e > e.expensive())
					->symmetricDifference(thisModule.Numbers(300)));

uses Helpers;
